#pragma once

// Bump this and tag/publish a GitHub release with the same version (e.g. "v1.0.1")
// whenever you want devices to pick up the build via the nightly OTA update check.
#define FIRMWARE_VERSION "1.0.0"
